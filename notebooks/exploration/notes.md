### Model Comparison
    - Random Forest
    - XGBoost Regressor
    - MLP

They had similar results on train/validation/interpolation dataset. The MLP generalizes better, the big difference is in extrapolation, specially in R², where it has positive value, while the others have negative values.

### About features
Correlation varies depending on the family.
