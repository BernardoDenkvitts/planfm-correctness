# MLP configuration - passed to train_correctness.py

# --- Experiment metadata ---
description: "Experiment using the chosen settings for the MLP"
experiment: final_experiment   # used as the folder name under the family directory

family: ad_lstm_wl_delta
seeds: [13, 23, 37]      # runs all seeds

# --- Model architecture ---
hidden_dims: [128, 64, 32]
dropout: 0.2
activation: GELU         # ReLU | GELU | SiLU

# --- Training ---
lr: 0.001
weight_decay: 0.0001
batch_size: 64
epochs: 150
patience: 20

# --- Feature selection (leave empty to use all 55 features) ---
features_to_drop: []     # drop features whose name contains these substrings
features_to_keep: []     # if non-empty, use ONLY these exact feature names

# --- Runtime ---
device: cuda             # auto | cpu | cuda | mps
verbose: false