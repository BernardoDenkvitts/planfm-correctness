# Blocks State Data

This folder contains the small Blocks subset used by the tests.

The checked-in trajectory is enough to verify gold-plan recovery without including the full raw trajectory corpus.
