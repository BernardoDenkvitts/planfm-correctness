# State Trajectories

This folder contains one small Blocks trajectory for smoke tests.

The full raw state-trajectory corpus is not included in this GitHub snapshot. I include the downstream candidate files in `data/validity_dataset/candidates`, so training and evaluation do not require the full raw trajectory corpus.
