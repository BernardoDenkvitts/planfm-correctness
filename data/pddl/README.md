# PDDL Files

This folder contains the PDDL domains and problem files used for symbolic rollout.

The downstream feature extractor needs these files because it grounds each planning problem with pyperplan and rolls out candidate action sequences before computing transition-model features.
