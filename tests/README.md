# Tests

This folder contains focused tests for the downstream plan-validity path.

## Test Coverage

- recover a gold Blocks plan from a state trajectory
- generate invalid candidate plans through corruption operators
- extract a finite frozen-source feature vector when the optional model dependencies are installed

The tests use the small Blocks trajectory in `data/states`.
